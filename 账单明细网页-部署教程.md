# 账单明细网页控制台 · 部署教程（终端版）

> 目标：让 Telegram bot 自带一个**可记账的账单明细网页**，手机点 Bot 卡片里的「📋 账单明细」按钮直达，
> 线上地址 `https://pg.count168.site/`。
> 架构：网页服务**内嵌在 bot 进程里**（新文件 `webconsole.py`，纯 Python 标准库，零新依赖），
> 记账口径与 Telegram 完全共用同一套代码（见 bot.py 里的 `create_ledger_entry`）。
> 服务器：srv1808383（Hostinger VPS，IPv6 `2a02:4780:5e:74bf::1`，已装 Docker，`ledgerbot_container` 已在跑）。
> 域名：`pg.count168.site` 已接入 Cloudflare（橙云代理）。

---

## 〇、原理（一句话）

bot 进程内多跑一个 HTTP 线程（端口 8787）→ Cloudflare 把 `https://pg.count168.site` 转发到 VPS 的 8787 →
手机点 Telegram 按钮直达网页。链接带**签名**（HMAC），只有管理员/操作员的链接能打开，同一份签名链接绑定一个群，改不了别人的账本。

---

## 一、本机上传（PowerShell，1 条命令）

```powershell
cd "C:\Users\senheng\Desktop\Tele Bot"
scp bot.py webconsole.py "root@[2a02:4780:5e:74bf::1]:/opt/ledgerbot/"
```

- `webconsole.py` 是新增的网页控制台，必须和 bot.py 放在同一目录。
- 提示输服务器密码（IPv6 地址要 `[方括号+引号]` 包起来，和以前一样）。

## 二、VPS 上改 Dockerfile（多拷一个文件）

```bash
cat > /opt/ledgerbot/Dockerfile <<'EOF'
FROM python:3.12-slim
WORKDIR /app
COPY bot.py webconsole.py .
RUN pip install --no-cache-dir "python-telegram-bot[job-queue]"
CMD ["python", "bot.py"]
EOF
```

## 三、VPS 上给 .env 追加网页配置（凭据只放 .env）

先生成一个签名密钥（在 VPS 终端直接跑）：

```bash
python3 -c "import secrets;print(secrets.token_urlsafe(32))"
```

把输出粘到下面第三行：

```bash
cat >> /opt/ledgerbot/.env <<'EOF'
WEB_CONSOLE_SECRET=把这里换成上面生成的密钥
WEB_CONSOLE_BIND=0.0.0.0
WEB_CONSOLE_PORT=8787
WEB_CONSOLE_BASE_URL=https://pg.count168.site
EOF
chmod 600 /opt/ledgerbot/.env
```

| 变量 | 说明 |
|---|---|
| `WEB_CONSOLE_SECRET` | 链接签名密钥，**不设 = 网页功能整体不启用**；换一个值 = 所有旧链接立即作废（吊销手段） |
| `WEB_CONSOLE_BIND` | 容器内监听地址，VPS 上必须 `0.0.0.0`（本机试跑用默认 `127.0.0.1`） |
| `WEB_CONSOLE_PORT` | 端口，默认 8787 |
| `WEB_CONSOLE_BASE_URL` | 按钮链接的域名前缀；不填会自动探测局域网 IP（家用试跑用） |

## 四、重建容器（改了代码和 .env 都必须 rm + run，restart 不生效）

```bash
cd /opt/ledgerbot
docker build -t ledgerbot_image .

docker rm -f ledgerbot_container
docker run -d --name ledgerbot_container \
  --restart unless-stopped \
  --env-file /opt/ledgerbot/.env \
  -p 8787:8787 \
  -v /opt/ledgerbot/data:/app/data \
  ledgerbot_image
```

和以前相比**只多了一个 `-p 8787:8787`**。

## 五、验证（三步）

```bash
docker ps --filter name=ledgerbot                    # STATUS 应为 Up
docker logs ledgerbot_container 2>&1 | grep 控制台    # 应出现「✅ 账单明细网页控制台已启动」
curl -s "http://127.0.0.1:8787/api/health"           # 期望 {"ok": true}
```

## 六、把域名接到 8787（Cloudflare 面板，一次性）

`pg.count168.site` 走的是 Cloudflare 橙云，默认只会回源到 80/443 端口；我们的网页在 8787，需要加一条「源站规则」：

1. Cloudflare 面板 → 选中域名 `count168.site` → 左侧 **规则 Rules → Origin Rules（源规则）** → 创建规则：
   - 匹配条件：主机名 等于 `pg.count168.site`
   - 目标端口（Destination Port）→ 重写为 `8787`
   - 保存并部署
2. 左侧 **SSL/TLS → 概述**：加密模式确认是 **灵活（Flexible）**（访客 https，Cloudflare 到源站 http）。
   如果之前因为别的原因设成了「完全」，要么改回灵活，要么后面给源站配证书——个人记账场景灵活模式即可。
3. 放行防火墙：Hostinger 面板 → Firewall → 放行 **TCP 8787**（如果之前开过 8080 的经验，一样的操作）。

> 现状参考：目前 `https://pg.count168.site` 打开是一个 301 循环（还没有源站在应答），按上面配好规则后就会指向我们的网页。

验证公网：

```bash
curl -s "https://pg.count168.site/api/health"        # 期望 {"ok": true}
```

**不想动 Cloudflare 的备选方案**：把第四步的 `-p 8787:8787` 换成 `-p 80:8787`（容器 8787 映射到宿主机 80 端口），
Cloudflare 灵活模式会直接回源 80，不用建 Origin Rules。前提是 VPS 的 80 端口没被占用（`ss -tlnp | grep :80` 检查）。

## 七、Telegram 里实测

1. 群里发「账单」（或 `/ledger`、`+100 备注`）→ 卡片下方出现 **「📋 账单明细」** 按钮
   ——现在这个按钮是**带签名的**：点开直接就是该群账单，顶部显示群名，可以**直接在网页上记账**。
2. 手机上：输入金额 → 点「＋ 入账」/「－ 出账」→ Telegram 里立刻能看到这笔（同一份账本）。
3. 非操作员点开按钮 → 显示「不在授权名单」；链接被改过一个字符 → 显示「链接无效或缺少签名参数」。

## 八、本机试跑（不部署，先在 Windows 上玩）

```powershell
cd "C:\Users\senheng\Desktop\Tele Bot"
$env:WEB_CONSOLE_SECRET = "随便一串密钥"
$env:BOT_TOKEN = "你的token"
python bot.py
```

日志会打印一行 `✅ 账单明细网页控制台已启动：http://<你电脑的局域网IP>:8787`。
手机连**同一个 WiFi** 点 Telegram 按钮即可打开（不用 Cloudflare，也不用填 BASE_URL，自动探测）。
打不开就查：防火墙放行 python 入站 → 链接里的 IP 是不是电脑当前 IP（重启程序会刷新）。

## 九、「待确认账单」（可选功能）说明

页面上的「待确认账单」区块读取数据目录里的 `pending_scans.json`（**文件不存在时区块自动隐藏**，现在没有就是隐藏状态）。
以后如果有 OCR/扫码管线，把解析结果按下面格式写进去即可，网页确认入账后**自动防重复**：

```json
{
  "-1001234567890": [
    {
      "scan_id": "20260915-0001",
      "merchant": "某商户",
      "amount": 88.5,
      "currency": "AUD",
      "transaction_id": "TXN-XXXX",
      "date": "2026-09-15",
      "time": "10:00",
      "status": "pending"
    }
  ]
}
```

- `scan_id` 唯一；入账时网页会把 `scan_id` 写进账本条目，同一张单**只能入账一次**（重复确认会被拒绝；
  管线把同一张单重新灌进来也会显示「已自动忽略」，不用任何人操作）。
- `status`：`pending` → 网页确认后变成 `confirmed:in` / `confirmed:out` / `ignored` / `duplicate`。

## 十、日常运维速查

```bash
docker logs -f ledgerbot_container                    # 实时日志（含网页启动行）
docker restart ledgerbot_container                    # 重启
# 更新代码：本机重新 scp 上传 bot.py / webconsole.py 后：
cd /opt/ledgerbot && docker build -t ledgerbot_image . && docker rm -f ledgerbot_container && docker run -d \
  --name ledgerbot_container --restart unless-stopped --env-file /opt/ledgerbot/.env \
  -p 8787:8787 -v /opt/ledgerbot/data:/app/data ledgerbot_image
# 换签名密钥（吊销所有旧链接）：改 .env 里 WEB_CONSOLE_SECRET 后同样 rm + run
```

## 十一、安全与边界

- 链接签名是**唯一门票**：未带签名/改过参数的请求拿不到任何数据（401）；不在管理员/操作员名单的拿不到（403）。
  密钥只存 `.env`，**不要提交进仓库**（本地 `.gitignore` 已排除 `.env`）。
- 传输走 Cloudflare 的 https；网页上所有写操作都要求操作员身份，且只能操作签名绑定的那一个群。
- 旧版只读 WebBot（`webbot_container`，端口 8080）**不受影响**，可以继续跑当备份查看器；
  不想要了就 `docker stop webbot_container`（数据在 bot 那边，网页层随时可删可重建）。
- 网页层任何异常都被兜住，**不会影响 bot 本身的记账**（控制台起不来只会在日志里报一行错误）。
