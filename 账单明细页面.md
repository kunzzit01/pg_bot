# 账单明细页面（只读记录页）· 业务说明书

> 本文档是「📋 账单明细」网页的**唯一定义文档**。页面由 `webconsole.py` 内嵌在 Bot 进程里提供，
> 手机从 Telegram 账单卡片的「📋 账单明细」按钮直达（线上 `https://pg.count168.site/`）。
>
> 定位：**只读账单记录页**——它只负责「看」，不负责「记」。记账一律在 Telegram 群里完成。

---

## 一、页面结构（从上到下）

```
┌ 账单明细                            [EN] [🌙] [⤓] ┐
│   （无返回键、无英文副标题、无币种徽章）             │
├ 日期卡：📅 2026-09-15 00:00  至  2026-09-23 23:59  ▾  ✕ │
│         （面板里只有两个时刻 + 日历）                │
├ 搜索卡：🔍 搜索金额 / 标记 / 操作人 / 备注…        ✕ │
│         chips：金额 · 标记 · 操作人 · 备注（无 chip 高亮=全列搜）│
├ 入账卡：🟩↓ 入账 5笔                    总计 +150  ▾ │
│   表：时间 / 金额 / 标记 / 操作人 / 备注              │
├ 下发卡：🟥⇩ 下发 2笔                   总计 -660  ▾ │
├ 分组卡：🟦▦ 分组 1组                   总计 -30   ▾ │
│   表：时间 / 代号 / 总入金额 / 总出金额 / 总账        │
├ 汇总卡：🟫▤ 总计 · 全期汇总                     ▾ │
│   总入账 +150                                        │
│   总下发 -660                                        │
│   总金额 -510                                        │
└ （页脚那行「🔒 账目记录已实时加密备份」已按老板要求去掉）      ┘
```

外观遵循设计稿：顶栏只有大标题与右侧三个按钮（语言 / 一键昼夜 / 导出），卡片圆角 16px、卡片头左侧色块图标、右侧该表「总计」，底部「总计」独立成卡。

卡片头左侧的色块图标是**自绘 SVG 线稿**：入账 = 箭头落入底托、下发 = 箭头向上离开顶托、分组 = 四个小方块、总计 = `Σ`；28×28 圆角色块 + 16px 图标、1.9px 白色描边（跟面板里的时钟图标同一套画法）。早先用 `↓ ⇩ ▦ ▤` 这类文本符号，字体不同长相也不同、也偏糙，已换掉。

**卡片可以收起**：点卡片头（整条都可点，键盘 Tab 聚焦后 Enter / Space 也行）就收起或展开这张卡，卡头最右端的 `▾` 会转成 `▸`。卡片头本身带着**笔数与总计**，所以收起来不丢关键信息 —— 只想看某一张表时，把其余三张收掉即可。收起状态记在 `localStorage`（key `ledger_folded`），刷新后还在；PDF / Excel 导出不受影响（导出的是完整数据，跟页面折叠状态无关）。

**下拉刷新**（手机）：页面已经在顶部时往下拉，顶部会出现一条「下拉刷新 → 松手刷新 → 刷新中…」的提示条，松手就重新拉一次当前日期区间/筛选条件下的数据（表格、笔数、总计一起更新）。没到阈值（约 54px）或页面已滚动时不会触发，往上划仍是正常滚动；日期面板打开时也不抢手势。手指点按时常见的几像素抖动不算「下拉」（判定阈值 12px，约合手指 22px），所以不会把随后的点击吞掉 —— 点空白处照常能收起候选列表。桌面端用 F5 / Ctrl-R 刷新即可。

**表格是「轻表格」**（图二那版）：表头一条暖米底色 + 上下各一条细线，**列与列之间有 1px 竖分隔线**（表头行里也有），每行下方一条细线做分隔，偶数行浅米底做斑马纹；**没有表格外框、也没有把每个单元格都框起来** —— 最左/最右不画竖线，所以不是外框。上一版那种「Excel 式满格线 + 外框」已按老板要求撤掉，但竖的分隔线要保留。

色带**四周都通栏到卡片边**：`.tw` 用负外边距抵消卡片左右内距（自身不留内距，否则整张表会缩进 12px、像浮在卡片里），底部再吃掉卡片那 4px 内距，让表格**收到卡片底边**；**最后一行不单独画线**（否则会和卡片边框凑成「细线 + 白边 + 边框」三层），超出圆角的部分交给卡片 `overflow:hidden` 按圆角裁切 —— 设计稿就是靠卡片圆角收口的。手机端卡片内距收成 8px 时，负外边距同步收成 -8px。

**三张表共用同一套列宽**：`table-layout:fixed` + 第 1 列（时间）22%、其余四列各 19.5%。所以入账 / 下发 / 分组的竖线落在同一批 x 位置、上下对齐（分组表列名不同，但同样铺在五个格子上）。

**全表左对齐**：列名与内容都从各自列的**左边缘**起排 —— 三张表的表头名字（含分组的「代号 / 总入金额 / 总出金额 / 总账金额」）因此全部对齐在同样的竖线右侧（原来「金额 / 总入金额」这类数字列是居中的，跟左对齐的「代号」看着参差）。
表格另设 `min-width:520px`：**窄屏（卡片比 520px 还窄时）不再挤压截断，而是由外层 `.tw` 横向滚动**，把后面的列滑出来看完整（时间列因此在手机上也是完整的 `09-15 13:32`，不会变成 `09-15 13…`）；屏幕够宽时仍是 `width:100%` 铺满，没有滚动条。
只有超长内容仍在自己那一列里处理：文字列（时间 / 标记 / 操作人 / 备注）`nowrap` + 省略号；**数字列放开换行**（`.amtn` 用 `white-space:normal`）——宁可折行也不把金额截成 `-1,2…`。列宽是百分比，手机到桌面都按同一比例缩放、三张表始终对齐。

- **大标题**固定为「账单明细」（英文 Ledger Detail）。**没有副标题行**（群名/账期都不显示：下面已经有日期了）。
- **顶栏只保留大标题与右侧三个按钮**：没有返回键、没有英文副标题、没有币种徽章。
  币种仍存在于数据里（导出的 PDF / Excel 抬头会写「币种：AUD」），只是页面上不再展示。
  换币种在 Telegram 里发「修改币种 AUD到MYR」，该指令会把整个账单（未撤销记录 + 结转 + 设置）一起换掉。
- **右上角两个切换**：`🌙/☀️` 夜间模式、`EN/中文` 中英切换，两者都用 `localStorage` 记住选择。
- **没有任何记账入口**：页面不提供「记一笔」「入账/出账」按钮，也不提供扫描单确认。旧的「快速记账」「汇总」「待确认账单」三个区块已按需求整体移除。

---

## 二、日期区间：一行显示，时刻在面板里设

页面只有一行（**放不下会自动换行：日期一排、时间整段落到第二排**，不再截断成「2026-09-15 00:00 至 2026-…」）：

```
📅 2026-09-15 00:00  至  2026-09-23 23:59        ▾
```

- **点一下**这一行打开面板；面板里只有 ①两个时刻输入（开始 / 结束）②日历 ③完成 —— 不再有「开始/结束 日期」那一行，也不再有提示语（老板嫌凌乱，已去掉）。
- **圈区间**：在日历里点两下 —— 第一下设为开始日期、第二下设为结束日期（自动纠正逆序）；只点一下表示「从这天起」。
- **要单日就再点一次那天**：`09-15 ~ 09-18` 想只看 18 号 → **再点一次 18 号**，两端自动收成 `09-18 ~ 09-18`；同理点回开始那天（如点 15 号）也会收成 `09-15 ~ 09-15`。点区间外的日子则是延长/缩短区间。
- **时刻是自绘的下拉选单（全 24 小时制）**：两个字段显示 `00:00` / `23:59`（跟表格里的时间格式一致），点字段就在**它正下方贴出一条小下拉**（宽度与字段对齐，不是弹窗、也不离开日期面板）——里面是**时 / 分两列滚动列表**（00–23、00–59），**点选即生效**（当前字段描边变焦糖色、默认结束 23:59 会自动滚到视野里）。再点同一字段＝收起，点另一个字段＝切过去，点别处 / Esc / 滚页面也收起；日期面板一收起（✕ / 完成 / 遮罩 / Esc）时刻下拉跟着一起收。
  不再使用浏览器自带的 `<input type="time">`（它的 12 小时 AM/PM 弹层又蓝又花，跟页面不是一套）。
- 改任一时刻或点日历都会**立即生效**（防抖 260ms），三张表跟着刷新。
- 开始/结束都为空 = 不按日期筛选，直接看整个当前账期（默认状态）。
- **窄屏会换行**：日期段与时间段各自是一个不可断开的块，一行放不下时整段折到下一排（时间落到第二排），任何宽度下都能完整读到区间，不会出现省略号。
- 行尾 **✕** 清除日期（并把「编辑端」复位到开始端，避免清空后第一下被当成结束日期）；日期为空时两个时刻不参与筛选。
- 有日期时区间端点取 `开始日期 00:00:00` 到 `结束日期 23:59:59`（时刻按面板里的设置）。

## 二点五、搜索与筛选（只筛显示的行）

搜索卡里**上面一排 chips、下面搜索框**（放大镜画在框内左侧）：`金额 · 标记 · 操作人 · 备注`（**没有「全部」chip**）。chips 是浅底药丸：悬停变浅焦糖底、按下去微微缩一下；**选中的填焦糖色、加一层柔和投影**（夜间为冰蓝底 + 内描边），一眼能看出当前筛的是哪一列。

- 输入关键字即过滤**入账 / 下发两张表显示的行**。
- **没有 chip 高亮 = 四列一起搜**（默认态）。点一个 chip 就把搜索限定到那一列，**再点一次同一个 chip 取消**、回到四列一起搜 —— 这是选完某列后回到「全部列」的路子（原来那个 `全部` chip 已按老板要求去掉）。取消时只把下拉收起、**不会再自动弹出来**（选中才弹）。
- **点 chip 就直接展开候选下拉**（一步到位，不用再去点搜索框）：点 `金额` 就列出金额取值、点 `操作人` 就列出操作人……展开时不聚焦输入框，所以手机上不会先弹键盘；想打字点一下输入框即可。`▾` 箭头也能开合。打字即过滤这个列表，**点一项就完成筛选**（输入框填入该值）并自动收起。
- **每个范围都能列候选值**：按当前范围取列值（没有 chip 高亮时是四列取值合并列出），按出现次数从多到少排、右边带笔数。
  - 「金额」列的是带符号的显示值（如 `+200` / `-480`），「标记」列的是被回复人，「备注」列的是备注原文；空值不列。
  - **浮层贴着输入框展开**：左右宽度与输入框对齐、挂在它正下方 6px。因为 chips 在搜索框**上面**，浮层不会盖住 chips —— 列表开着也能直接点另一个 chip 换范围。点页面别处（用 `pointerdown` 判定，鼠标与触摸都灵，不依赖 click 合成）、按 Esc、滚动页面都会收起；下方空间不够（手机键盘弹起时）自动翻到输入框上方。
- **只筛行，不动数字**：三张卡头的「总计」、分组表、底部汇总仍是**当前日期区间**的合计 —— 关键词不会让总额跳动。
- 有搜索时，卡头计数显示 `命中 / 总数`（例如 `入账 1 / 5 笔`）；没有命中显示「没有匹配的记录」。
- 行尾 **✕** 清除关键字（不清除已选的搜索范围）。
- 分组表不参与搜索（它是按代号汇总的视图）。

## 三、三张表的字段与口径

三张表的数字**同源**：都基于「当前账期 + 当前日期区间」这一份筛选结果计算，保证页面上看到的数字互相自洽。

### 表一 · 入账
| 列 | 来源 | 说明 |
|---|---|---|
| 时间 | `time` | 显示 `MM-DD HH:MM`，账本里存的是群本地时区的时间字符串 |
| 金额 | `net_amount` | `+金额` 显示为正、`-金额` 显示为负（与 Telegram 卡片「已入账」同口径） |
| 标记 | `reply_user_name` | **被回复人**；没有回复任何人时显示 `—` |
| 操作人 | `operator_name` | 发消息的人（`@用户名` 或昵称） |
| 备注 | `note` | 记账时写在金额后面的文字 |

入账表包含 `+`（入账）与 `-`（出账）两种记录，按时间正序排列。

### 表二 · 下发
列与入账表相同，只包含 `下发` 记录。金额取 `net_amount`：

- `下发 2000 手续20` → 金额显示 `-1980`
- 冲正 `下发 +2000` → 金额显示 `+2000`，并在金额后追加「冲正」小字标签

### 表三 · 分组
**一行 = 一个代号**（Telegram 里 `KY +50 备注` 那个 KY）。

| 列 | 口径 |
|---|---|
| 时间 | 该代号**最近一笔**记录的时间 |
| 代号 | `entry["group"]`；无代号的下发/记账不进此表 |
| 总入金额 | 该代号 `+金额` 记录的**原始金额**之和（不扣手续费） |
| 总出金额 | 该代号 `-金额` 的原始金额之和 **+ 该代号下发的净流出**（已扣手续费；冲正会回冲成负贡献） |
| 总账 | 总入金额 − 总出金额 |

- 只统计**未撤销**的记录。
- Telegram 卡片里的「分组统计」是同一份口径的净额版（`代号 👉 净额`），只算 `+/-` 记账、不算下发；网页版按需求把下发也纳入代号，并拆成「总入 / 总出」两列。
- 分组卡的卡头「总计」= **各组总账之和**（分组口径的合计）。
- **已撤销（voided）的记录整条不出现**：不列出、不计入任何数字、**也不加任何提示**（原来标题后那句「（另有 N 笔已撤销）」已按老板要求去掉），与 Telegram 账单卡片、日切口径一致。
- 账期日切后明细会被清空，页面只展示当前账期的记录（归档汇总请用 Telegram 的「全局账单 MM-DD」查看）。

---

## 四、「标记」列 = 被回复人（新增字段）

### 业务含义
操作员**回复群里某个真人的消息**，然后发 `+100`，这条流水就会记下被回复人：

```
客户A：我转账了
操作员：（回复客户A那条消息）→ +100 已收
        ↑ 该笔的「标记」列显示 @客户A
```

### 记录规则（`bot.py: extract_reply_target()`）
| 场景 | 是否记录 |
|---|---|
| 回复**其他真人**的消息后记账 | ✅ 记 `reply_user_id` / `reply_user_name` |
| 回复 **Bot 自己**的消息（如记账卡片） | ❌ 不记（避免标记列全是机器人） |
| 回复**自己**的消息 | ❌ 不记 |
| **没有回复**，直接发 `+100` | ❌ 不记，标记列显示 `—` |

### 重要限制
- **只对新记录生效**：历史上已有的流水没有这个字段，标记列显示 `—`，不做回填。
- `+金额`、`-金额`、`下发` 三种记账都会记录被回复人。
- 该字段**不参与**任何统计，也不影响「撤销」的匹配逻辑（撤销仍按消息 ID 匹配被回复的那条记账消息）。

---

## 五、下发支持代号（写法变更）

| 写法 | 结果 |
|---|---|
| `下发 2000` | 旧写法，仍然可用；无代号 |
| `下发 2000 手续20 备注` | 旧写法，仍然可用 |
| `KY 下发 2000` | **新增**：该下发记入代号 KY，出现在分组表 |
| `KY 下发 2000 手续20 备注` | 新增：代号 + 手续费 + 备注 |
| `KY 下发 +2000` | 新增：代号 + 冲正 |

- 正则 `RE_LEDGER_DISBURSE`（`bot.py`）已改为可选代号前缀。
- 代号规则与 `KY +50` 保持一致：不含空白与 `+`/`-` 的一段文字。
- 守卫逻辑保持：`下发` 关键字不会被当代号（发 `下发 +2000` 仍走下发分支）。

---

## 六、账期与历史数据（硬约束）

| 概念 | 规则 |
|---|---|
| 当前账期 | 由 `period_start` 界定（`ledger_settings.json`），页面默认展示它的全部记录 |
| 日切（结束账单） | **会把当期流水物理清空**，同时把汇总写入 `global_bill_archive.json`（结算额/总进/总出/笔数/币种） |
| 历史账期 | **只有当日汇总，没有明细**——这是 Bot 的设计，不是页面缺功能。页面用一行归档数据表示 |

> 因此：页面上「开始/结束」区间能查的明细，**只存在于当前账期内**。

---

## 六点五、主题与界面标准（可访问性，按 HIG/WCAG 校准）

**两套主题完全解耦**（不是同一套色反相）：

| | 白天 · Warm Caramel & Ivory | 夜间 · Deep Midnight |
|---|---|---|
| 页面底 | `#FFF8F5` 暖米白 | `#0B0F19` 极夜冷海蓝 |
| 卡片 | `#FFFFFF` + 暖褐细边 `#F0E6DE` + 暖影 | `#131B2E` + 冷边 `#22304C`（无阴影） |
| 表头 / 斑马 | 表头 `#F2E9E2`、斑马行 `#F6F0EA` 暖米 | `#1E293B` / `#1A233A` 深冷暗调 |
| 主文字 / 次要 | `#261E1A` 黑咖啡 / `#726659` 暖褐灰 | `#F8FAFC` / `#94A3B8` |
| 入账 | `#14783A`（徽标底 `#16A34A`） | `#34D399`（徽标底 `#10B981`） |
| 支出 / 下发 | `#C81E1E`（徽标底 `#DC2626`） | `#FB7185`（徽标底 `#F43F5E`） |
| 品牌 / 币种胶囊 | 焦糖 `#B86B3D`（胶囊底/徽标） | 冰蓝 `#38BDF8`（微光底 + 文字） |
| 代号 KY / 标记 @ | 琥珀 `#8A5300` on `#FDF1DC` | 冰蓝 `#38BDF8` on `#0E2A3A` |
| 分组徽标 | 深板岩 `#55688A` | 冰蓝 `#38BDF8` |

> 为满足小字 ≥4.5:1，个别色值沿同色系微调：白天绿 `#16A34A`→**`#14783A`**（文字）、白天红 `#DC2626`→**`#C81E1E`**（文字，斑马行上才达标）、夜间玫红 `#F43F5E`→**`#FB7185`**（文字）、焦糖 `#B86B3D`→**`#9F5830`**（文字与胶囊底）、琥珀 `#C27803`→**`#8A5300`**（文字）、暖褐灰 `#786C65`→**`#726659`**（表头字与次要文字，底色加深后重新达标）。徽标底色仍用色卡原值（图形按 3:1 标准）。

**字体**：`"Plus Jakarta Sans", Inter, -apple-system, …` 字体栈 —— 设备装了就用，没装自动回退系统字体（不引外链字体，保持离线可用）。数字统一 `tabular-nums` 等宽对齐。

其余可访问性标准（在渲染页面上实测）：

| 项 | 标准 | 实测 |
|---|---|---|
| 正文/小字对比度 | <18pt 需 ≥4.5:1 | 白天最低 4.60、夜间最低 5.71（全项零失败） |
| 最小字号 | 移动端 ≥11pt | 全页最小 11px |
| 点击目标 | 推荐 44×44，最小 28×28 | 日期/清除 48、语言/昼夜/导出 44、时间 44、日历格 47 |
| 颜色不是唯一线索 | 状态要有替代表达 | 入/出用 `+/-` 符号 + 颜色；已撤销不显示 |
| 加载反馈 | 请求期间要有状态 | 首屏三张表显示「加载中…」并置 `aria-busy` |
| 屏幕阅读器 | 控件需有可访问名称 | 图标按钮带 `aria-label`；表头 `scope="col"`；错误提示 `role="alert"` |
| 安全区 | 刘海屏不被系统栏遮挡 | 顶栏 `padding-top: env(safe-area-inset-top)` |

### 底部「总计」卡片的口径

| 指标 | 算法 |
|---|---|
| **总入账** | = 入账表的合计（含 `- 记一笔`，即净存入），已撤销不计 |
| **总下发** | = 下发表净额，**负数显示**（下发金额已扣手续费，冲正会回冲），已撤销不计 |
| **总金额** | = 总入账 + 总下发 |

> 入账表里 `-50` 这类「记一笔出账」本来就在入账表内（作为减项），下发是单独一张表；分组只是这些记录的拆分，所以这个口径**已经包含了分组**。页面、PDF、Excel 三处用的是同一套数字（已逐一对过）。汇总卡为三行：`总入账 +150 / 总下发 -660 / 总金额 -510`。

## 六点八、导出（PDF / Excel，用户自己选）

顶栏夜间模式右边是导出按钮（`⤓` 图标，44×44）。**点一下弹出两个选项**：

- **📄 导出 PDF** —— 走浏览器「打印 → 另存为 PDF」，内容见下（就是账单明细的干净版）。
- **📊 导出 Excel** —— 直接生成真正的 `.xlsx` 文件下载。

### 导出 PDF



导出按钮在**顶栏夜间模式按钮的右边**（`⤓` 图标，44×44，读屏标签「导出 PDF」）。它**不打印网页的卡片外观**（没有圆角卡片、彩色徽章），而是把当前账单明细按打印样式生成一份干净的文档，走浏览器自带的「打印 → 另存为 PDF」，不引入任何第三方库。

内容与页面完全一致（数字逐一核对过）：

```
账单明细
澳洲一号厅 · AUD · 2026-09-15 00:00 ~ 2026-09-23 23:59
────────────────────────────────────────────────
入账 6 笔                                  总计 +450
时间         金额    标记       操作人   备注
09-15 13:32   +200   —         John     —
…
────────────────────────────────────────────────
下发 2 笔                                  总计 -660
时间         金额              标记   操作人   备注
09-15 14:05   -480 (手续费 20)  —     John    下发 John
────────────────────────────────────────────────
分组 1 组                                    总计 -30
时间         代号   总入金额   总出金额   总账
09-15 16:20   KY     150        180       -30
════════════════════════════════════════════════
总计   总入账 +150   总下发 -660   总金额 -510
导出时间 2026-09-23 22:04
```

- **不摆架子**：没有序号列、小计行、报告期间/有效笔数、作废统计等正式报表元素。
- **与页面数字一致**：每段「总计」= 页面上该卡片的合计；底部「总入账/总下发/总金额」= 页面上汇总卡三行的值（总下发给负数）。
- **已撤销记录不列出、不计入、也不加提示**（页面上原来那句提示已去掉，PDF / Excel 里也不写）。
- 金额格式与页面一致（`+200` / `-50`），时间同页面（`MM-DD HH:MM`）。
- 打印样式：细线表 + 浅底表头 + 条目斑马纹、全黑白、`thead` 跨页重复、行不跨页断裂。
- 导出的是**当前筛选结果**（日期区间/时间生效中），文件名与内容都会带上期间，方便对账留痕。
- 文案跟随语言（中文导出中文，英文导出英文）。
- Telegram 内置浏览器部分 WebView 不实现 `window.print()`；点了没反应时，用 Telegram 菜单里的「在浏览器中打开」再导出。

### 导出 Excel（.xlsx，带 Excel 排版）

- **纯手写生成**：不引第三方库、离线可用（自己实现 OOXML + 样式表 + 存储式 ZIP + CRC32）。
- **4 个工作表**，与页面口径一致：

  | 工作表 | 列 | 末行 |
  |---|---|---|
  | 入账 | 时间 / 金额 / 标记 / 操作人 / 备注 | 小计（N 笔） |
  | 下发 | 时间 / 金额 / 手续费 / 净额 / 操作人 / 备注 | 小计（N 笔） |
  | 分组 | 时间 / 代号 / 总入金额 / 总出金额 / 总账 | 合计（N 组） |
  | 总计 | 总入账 / 总下发 / 总金额 + 群名 · 币种 · 期间 | — |

- **Excel 排版**（不是裸数据）：
  - 第 1 行**报表标题**（群名 · 账单明细 · 币种，14pt 加粗）+ 第 2 行期间/导出时间；
  - 表头行**深蓝底白字加粗**，并**冻结在表头下方**（滚动时表头常驻）；
  - 各列设了**列宽**（时间 19 / 金额 13–15 / 备注 30）；
  - 金额列用数字格式 `#,##0.00;[Red]-#,##0.00` —— 千分位、两位小数、**负数自动红色**；
  - 数据行带**细下边框**，小计/合计行**加粗 + 浅灰底纹**（Excel 报表的常见样式）。
- **数字是真数字**（可直接求和/排序/做透视），文字用内联字符串，中文完好（已解包校验：ZIP 完整、四表 XML 合法、样式索引无越界、单元格值与样式号正确）。
- 已撤销记录不导出、也不加说明；文件名形如 `账单明细_2026-09-15_20260923.xlsx`。

## 七、鉴权与安全（未改动）

- 链接带 HMAC 签名：`?id=<base64url(user_id:chat_id:username)>&t=<签名>`，签名把身份与群绑定，改一个字符即失效。
- 只有管理员/操作员名单内的账号能打开；不在名单 → 403；签名无效 → 401。
- 页面本身不含任何数据，所有数据都通过 `/api/*` 带签名拉取；轮换 `WEB_CONSOLE_SECRET` 可吊销全部旧链接。
- 页面为**只读**：不提供任何写接口调用（`/api/ledger/add`、`/api/scans/confirm` 仍存在于服务端，但页面不再使用）。

---

## 八、接口与实现对照

| 前端行为 | 接口 | 后端实现 |
|---|---|---|
| 加载群名 | `GET /api/session` | `bot.py: _console_authorized / settings_view` |
| 加载三张表 | `GET /api/ledger?period=&start=&end=` | `bot.py: _console_period_view` |
| 分组表数据 | 同上返回的 `groups` | `bot.py: _console_group_rows` |
| 条目字段裁剪 | 同上返回的 `entries` | `bot.py: _console_entry_view` |
| 历史账期列表 | 同上返回的 `history_periods` | `bot.py: _console_periods` |

部署提醒：改动 `bot.py` / `webconsole.py` 后必须重新 `docker build` + `docker rm -f` + `docker run`，`restart` 不生效。
