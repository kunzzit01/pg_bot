# Telegram Bot 核心功能文档

> 代码入口：`bot.py`（单文件，约 3600 行）
> 更新日期：2026-09-09（依据 9 月 8 日最新代码整理）
> 运行方式：双击 `启动测试.bat`（调用 `run-bot.ps1`：自动建 `.venv`、装依赖 `python-telegram-bot[job-queue]` + `openpyxl`、启动并监控日志）
> 技术栈：python-telegram-bot v20+（ApplicationBuilder / ConversationHandler / JobQueue），数据全部为 `data/` 下的 JSON 文件，时区基准 UTC+8

---

## 一、系统概述

这是一个**多租户商用群管机器人**（按"到期日期"对外出租），核心能力分五大块：

1. **授权与用户管理** —— 控制谁能用这个 Bot
2. **群发广播系统** —— 目标群管理 / 文案库 / 定时与即时群发
3. **租户到期管理** —— 设备/合约到期提醒、续租、退租
4. **记账账单系统** —— 群内实时记账（入账/出账/下发）、账期结算、多币种
5. **辅助功能** —— 新人欢迎语、简易计算器、Bot 到期续费（面向客户）

所有功能通过 **斜杠命令**、**主菜单按钮** 和 **中文关键词** 三种方式触发。

---

## 二、权限体系

| 角色 | 判定方式 | 能做什么 |
|---|---|---|
| 管理员 | `bot.py` 内写死的 `ADMIN_USERNAMES`（IgAccJohn、Dragonball77、MrK6776、IgCsLyn、jiang9546、react249） | 全部功能，含用户管理、群发、租户管理、清空账单、自动结算、全局账单 |
| 授权用户 | `data/authorized_users.json`（Telegram ID 或 username 列表） | 记账、查看账单、租户列表、提醒里的续租/退租按钮 |
| 未授权用户 | — | 一切消息被静默忽略 |

- 授权管理命令：`/adduser`（问答式添加 @用户名 或 数字ID）、`/listusers` / `/removeuser`（分页列表，点击用户→二次确认→移除）。
- **Bot 整体到期开关**：`data/bot_expire.json` 里的 `expire_date`（只能部署者在服务器后台改，Telegram 端只读）。到期后 Bot 对所有人静默，**唯一例外**是"联系客服"和"续费空间"两个入口（走 `is_authorized_ignoring_expiry`），保证客户永远能看到续费渠道。

---

## 三、主菜单与全局入口

`/start` 呼出主菜单键盘（ReplyKeyboard）：

| 按钮 | 行为 |
|---|---|
| 使用说明 | 输出全部命令帮助（HTML 排版） |
| 联系客服 | 回复管理员 @IgAccJohn（过期后仍可用） |
| 个人中心 | 显示本人 ID、用户名、授权状态 |
| 群发广播 | 提示管理员使用 `/broadcast` |
| 续费空间 | 显示到期日 + 剩余天数 + 续费套餐（过期后仍可用） |
| 全局账单 | 仅管理员：汇总所有群的当日净额 |

**续费套餐**（写死在代码里）：1个月 100U / 3个月 230U / 6个月 400U / 1年 700U，按钮点击后给出 USDT-TRC20 收款地址，并提示把转账截图发给客服确认后人工延长到期日。

---

## 四、群发广播系统

### 4.1 目标群管理（/addtarget、/listtargets、/removetarget）

- Bot 收到任何群/频道消息会自动把该群记入 `data/known_groups.json`（自动追踪，供后续按钮选择）。
- `/addtarget` 四步流程：**选群**（从已知群列表点选，或手动输 ID，自动拉取真实群名）→ **选分组**（多选勾选已有分类，也可直接输入新分类名）→ **备注名**（可跳过，默认用真实群名）→ 保存进 `data/broadcast_targets.json`。
- `/listtargets` 管理界面：
  - **分类视图**：按分类列出，点进分类进入"勾选矩阵"（编号按钮批量勾选成员，💾 保存生效）；
  - 可新建分类、刷新真实群名（🔄）、逐个移除目标（带确认）、分页浏览。

### 4.2 文案库（/adddraft、/listdrafts）

- `/adddraft`：两步——输入文案名称 → 输入文案内容，存入 `data/broadcast_drafts.json`。
- `/listdrafts`：分页浏览文案（带预览），点击 → 确认删除。

### 4.3 群发流程（/broadcast）

四步对话式流程：

1. **时间方式**：🚀 立即发送 ／ ⏰ 管理定时任务（单次 `YYYY-MM-DD HH:MM` 或每日 `HH:MM`，定时方案存 `data/broadcast_schedules.json`，可选用/删除/新增）
2. **目标分组**：点选某个分类，或"全部分组"
3. **文案来源**：临时现写新文案 ／ 从文案库选模板
4. **确认页**：汇总时间、分组、文案标签、内容预览 → ✅ 启动

发送逻辑（`do_broadcast`）：逐群发送、每条间隔 0.05 秒；自动处理**群升级为超级群后的 ID 迁移**（ChatMigrated，发送成功后自动更新 targets 里的群 ID）；失败逐条记录原因；完成后向发起管理员汇报"成功 X / 失败 Y"。

---

## 五、租户到期管理（duedate）

面向"设备/合约租户"的到期跟踪，数据存 `data/tenants.json`（按群分组）。

### 5.1 登记（/duedate，管理员）

1. 选群（从已知群点选）
2. **批量录入**：一行一个项目，格式 `设备名 开始日期(YYYY-MM-DD) 金额 合约期数`，可分多条消息陆续输入，错行单独提示重填
3. 选**续费周期**：一次性 / 每月 / 每周 / 每年 / 自定义天数
4. 系统自动算出到期日 `due_date`、当前期数 `current_period=1`、状态（合约进行中 / 已满最低期续租中），并记录登记人 username（用于后续 @提醒）

### 5.2 查看与管理

- 群内发关键词 **「租户列表」**：按到期日排序列出本群全部项目（名称/到期日/金额/合约进度）。
- `/listduedate`（管理员）：群列表 → 项目勾选矩阵，支持批量**退租**（不可恢复，二次确认）、单选项目后**编辑金额或合约期数**。

### 5.3 自动提醒与自动续租（每日定时扫描，UTC+8）

| 时间 | 行为 |
|---|---|
| 12:00 | 到期前 1 天 → 群里发"⏰ 明日到期提醒"+ 项目详情，附 **✅续租 / 🚪退租** 内联按钮 |
| 15:00 | 提醒后仍无人处理 → 升级提醒，**@登记人** 催促处理 |
| 次日 12:00 | 已过期 1 天且无人处理 → **系统默认自动续租**（期数+1、按周期推算新到期日），并在群里播报 |

- 续租/退租按钮仅授权用户可点，按钮上会记录操作员。
- `/ddtest1200`、`/ddtest1500`：管理员手动触发两次扫描，便于测试。

---

## 六、记账账单系统（核心模块）

每个群独立一套账（`data/ledger_entries.json` 按群 chat_id 分桶），群级设置存 `data/ledger_settings.json`。直接在群里用自然语言记账。

### 6.1 记一笔

| 消息 | 效果 |
|---|---|
| `+金额 [备注]` | **入账**：净额 = 金额 × (1 − 入账费率%) |
| `-金额 [备注]` | **出账**：净额 = 金额 × (1 + 出账费率%) |
| `下发 ±金额 [手续X] [备注]` | **下发记录**：净额 = 金额 − 手续费。默认只有**负数下发**才扣群设置里的固定手续费；写 `手续X` 则本笔强制按 X 扣（正负都生效） |

每笔记录自动带：流水号、类型、金额、费率、净额、汇率、换算后金额、币种、备注、操作人（@username）、精确时间、原始消息 ID 和 Bot 回执消息 ID。记账后 Bot 立即回复当前账单摘要作为回执。

支持全角/中文标点自动纠错（＋－×÷、全角数字、（）、。等都会归一化后解析）。

### 6.2 群记账设置（管理员和授权用户均可设置）

| 关键词 | 说明 |
|---|---|
| `设置入账费率 X` | 入账扣费百分比 |
| `设置出账费率 X` | 出账加收百分比 |
| `设置[币种]汇率 X` | 指定币种换算：`×X` 乘法 或 `÷X` 除法；没设置的币种默认 ×1 |
| `设置币种 XXX` | 本群记账币种（如 USDT、RM） |
| `设置时区 X` | 本群时区偏移（默认 UTC+8） |
| `设置手续费 X` | 下发固定手续费（负数下发时扣） |
| `记账设置` | 查看当前参数（⚠️ 当前代码有 bug，见第九节） |

### 6.3 查看账单

- 关键词 **「账单」** 或 `/ledger`：紧凑摘要视图——
  - 账期日期；已入账 / 已出账 / 已下发 各显示笔数 + 最近 5 笔（时间 + 金额 + 费率上标 + 净额 + 备注）；
  - 当前费率、汇率、下发手续费；
  - 今日累计入账 / 累计出账 / **GrandTotal**（= 结转余额 + 入账 − 出账 + 下发）；
  - **多币种自适应**：本期出现 ≥2 种币种时，自动切换成按币种分行显示。
  - 超过 5 笔出现「🔽 展开查看更多」→ 按时间合并、每页 20 笔分页翻阅（历史日期也能查），「🔼 收起」返回紧凑视图。

### 6.4 账期（不是自然日，靠"结束账单"推进）

- 账期起点在第一次记账（或第一次查询）时确定，之后**只有手动/自动结算才会推进**，不随日期自动跳。
- 每笔记录写入时打上当期 `period_label`（YYYY-MM-DD）标签，历史账期按标签查询。
- `设定日期 YYYY-MM-DD`：人工校准账期显示日期。

### 6.5 结算与结转

| 关键词 | 行为 |
|---|---|
| `结束账单` | 结算本期各币种 GrandTotal → 写入该群**结转余额**（`ledger_carryover.json`）→ 账期标签 +1 天、账期起点重置为当前时刻。结算前自动存快照 |
| `撤销结束账单` | 用快照恢复到结算前状态（可撤一次） |
| `清空账单` | 仅管理员，二次确认按钮：当期所有未作废记录批量作废（结转余额不动），存快照 |
| `撤销清空账单` | 恢复最近一次被清空的那批记录 |
| 回复某笔记账消息发 `撤销` | 单笔作废（不计入统计），并回显最新账单 |
| 回复某笔记账消息发 `撤销恢复` | 单笔恢复 |

### 6.6 导出与自动结算

- **`导出账单`**：生成 Excel（openpyxl，Arial 字体、表头加粗、自适应列宽），导出**近 30 天**全部有效记录（排除已作废），文件名 `账单导出_时间.xlsx`。
- **`设置自动结算时间 HH:MM`**（仅管理员，全局生效）：每天该时间对所有有记账数据的群自动执行"结束账单"，并把结算结果发到各群；存 `data/ledger_global.json`，重启后自动恢复。
- `取消自动结算`：移除该定时任务。

### 6.7 全局账单（主菜单按钮，仅管理员）

遍历所有已知群：当日净额 = 入账 − 出账 + 下发，按正负分成 🔺正数群 / 🔻负数群 列表，统计群数、正数总额、负数总额、合计总额。

---

## 七、新人欢迎（全局）

| 关键词 | 说明 |
|---|---|
| `设置欢迎语 内容` | 全局欢迎语（所有群共用），支持 `{name}`（新人昵称）、`{group}`（群名）变量 |
| `开启欢迎` / `关闭欢迎` | 全局开关（默认开启） |
| `查看欢迎语` | 查看当前状态与模板 |

默认模板：`欢迎 {name} 加入 {group}！🎉`。新成员进群自动回复（跳过 Bot 自己）。

---

## 八、实用工具

- **简易计算器**：直接发算式（如 `3+5*2`），只允许数字和 `+ - * / ( ) .`，不支持幂运算 `**`，长度 ≤100，除零有友好提示。
- **@Bot 提及剥离**：群内 `@机器人名 指令` 等价于直接发指令。
- `/whereami`（管理员）：查当前聊天室 ID。

---

## 九、后台定时任务汇总

| 任务 | 触发 | 内容 |
|---|---|---|
| 租户扫描（上午） | 每日 12:00 UTC+8 | 到期前提醒 / 过期自动续租 |
| 租户扫描（下午） | 每日 15:00 UTC+8 | 升级提醒并 @登记人 |
| 记账自动结算 | 每日设定时间 UTC+8 | 所有群结束账单并播报（可选，需手动开启） |
| 群发定时任务 | 单次 / 每日 | `/broadcast` 里配置的群发 |

## 十、数据文件一览（data/）

| 文件 | 内容 |
|---|---|
| `authorized_users.json` | 授权用户（ids + usernames） |
| `bot_expire.json` | Bot 服务到期日（仅服务器后台可改） |
| `known_groups.json` | Bot 见过的所有群/频道（自动追踪） |
| `broadcast_targets.json` | 群发目标（分组、备注、真实群名） |
| `broadcast_drafts.json` | 群发文案库 |
| `broadcast_schedules.json` | 群发定时方案 |
| `tenants.json` | 租户项目（按群，含到期日/周期/期数/状态/登记人） |
| `ledger_settings.json` | 各群记账参数（费率、汇率、币种、时区、账期） |
| `ledger_entries.json` | 各群全部记账流水 |
| `ledger_carryover.json` | 各群结转余额（按币种） |
| `ledger_close_snapshot.json` | "结束账单"快照（供撤销） |
| `ledger_clear_snapshot.json` | "清空账单"快照（供撤销） |
| `ledger_global.json` | 全局自动结算时间 |
| `welcome_settings.json` | 全局欢迎语配置 |

## 十一、已知问题与注意事项

> 2026-09-09 已修复：「记账设置」读取旧字段 `rate_mode`/`rate` 导致 KeyError 的问题（已改为按币种 `rates` 字典显示）；清理了 `try_handle_ledger_disburse` 中 `return True` 之后的死代码。

1. **群发定时任务重启后失效**：`/broadcast` 建的每日/单次任务只在内存 JobQueue 里，重启后不会从 `broadcast_schedules.json` 自动恢复，需要重新"选用"一次（对比：记账自动结算和租户扫描重启后会恢复）。
2. **Token 配置**：Token 已改为从环境变量 `BOT_TOKEN` 或根目录 `token.txt` 读取（`token.txt` 不入库，公开仓库不会泄露 Token）。
3. **流水号可能重复**：`entry.id = 当前条数+1`，作废的记录仍占号，多条撤销后重记会产生重复 id（撤销恢复按 id 匹配，极端情况下可能误恢复多笔）。
4. 启动日志里的 `PTBUserWarning`（per_message=False）为 python-telegram-bot 的正常提示，不影响功能。
